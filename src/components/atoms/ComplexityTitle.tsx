import styled from 'styled-components';

const HeadingComplexityTitle = styled.h6`
color: #111;
font-size: 16px;
font-weight: 400;
line-height: 18px;
display: block;
margin: 0; 
padding: 0; 
`;

function ComplexityTitle({ title }): JSX.Element {
  return (
    <HeadingComplexityTitle>

    </HeadingComplexityTitle>
  );
}

export default ComplexityTitle;
